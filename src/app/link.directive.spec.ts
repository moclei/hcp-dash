import { LinkDirectiveDirective } from './link.directive';

describe('LinkDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
